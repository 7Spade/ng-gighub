# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com/project/_/settings/api

# IMPORTANT NOTE FOR ANGULAR:
# The NEXT_PUBLIC_ prefix is maintained for compatibility with external tools and services.
# In Angular SSR context (server-side), these variables work as expected via process.env.
# For client-side Angular code, you may need to configure Angular environment files
# (src/environments/) or use angular.json file replacements for build-time injection.

# Public Supabase URL (safe for client-side code)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_URL=https://your-project.supabase.co

# Public anonymous key (safe for client-side code with RLS enabled)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (KEEP SECRET - server-side only, has full database access)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Secret (KEEP SECRET - used for token verification)
SUPABASE_JWT_SECRET=your-jwt-secret-here

# PostgreSQL Database Configuration
# These are typically provided by Supabase or your database hosting provider

# Direct database connection URL (pooled)
POSTGRES_URL=postgresql://user:password@host:5432/database

# Direct database connection URL (non-pooled for migrations)
POSTGRES_URL_NON_POOLING=postgresql://user:password@host:5432/database

# Prisma-specific connection URL (if using Prisma ORM)
POSTGRES_PRISMA_URL=postgresql://user:password@host:5432/database?pgbouncer=true

# Individual PostgreSQL connection parameters
POSTGRES_HOST=your-db-host.supabase.co
POSTGRES_DATABASE=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-database-password
