/**
 * Account Repository 介面
 * 
 * 定義帳號聚合根的持久化操作
 * 由基礎設施層實作
 */
export interface IAccountRepository {
  // TODO: 定義 repository 方法
  // - findById(id: AccountId): Promise<AccountAggregate | null>
  // - findByUsername(username: string): Promise<AccountAggregate | null>
  // - save(account: AccountAggregate): Promise<void>
  // - delete(id: AccountId): Promise<void>
}
