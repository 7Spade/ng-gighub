import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/** Shared Module - 包含共用的元件、directives、pipes */
@NgModule({
  imports: [CommonModule],
  exports: [
    // TODO: 匯出共用元件
  ],
})
export class SharedModule {}
